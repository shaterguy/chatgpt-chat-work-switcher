{
  "name": "chatgpt-request-snapshot-calibrator",
  "version": "0.2.0-dev1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/snapshot-core.js && node --check src/bridge.js && node --check src/content.js && node --check src/popup.js && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
