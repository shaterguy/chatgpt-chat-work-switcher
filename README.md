# ChatGPT Chat ↔ Work Switcher

Chrome extension research/implementation project for switching execution mode inside an existing ChatGPT conversation while preserving the conversation ID and message tree.

Development happens on feature branches; `main` is kept as the stable base.
